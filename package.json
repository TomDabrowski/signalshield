{
  "name": "signalshield",
  "version": "0.1.0",
  "private": true,
  "description": "Shared filtering engine and browser extension for YouTube and Google Search.",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "node scripts/build.mjs",
    "typecheck": "tsc --noEmit",
    "test": "tsx --test tests/**/*.test.ts",
    "check": "npm run typecheck && npm run test"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.24",
    "esbuild": "^0.25.1",
    "tsx": "^4.20.5",
    "typescript": "^5.9.2"
  }
}
